# CreatorReach AI 正式团队使用说明

这份文件夹是已脱敏交付版：不包含个人 Gmail 授权、OpenAI API Key、飞书 Webhook、达人邮箱、历史回复、Gmail 草稿记录或错误日志。

## 1. 启动系统

进入项目目录：

```bash
cd /Users/baichaoyu/Desktop/CreatorReach AI_正式团队交付版
python3 app.py
```

默认访问：

```text
http://127.0.0.1:8000/
```

如果 8000 端口被占用：

```bash
PORT=8001 APP_BASE_URL=http://127.0.0.1:8001 python3 app.py
```

## 2. 环境变量配置

复制 `.env.example` 里的配置方式。也可以直接在启动命令前设置：

```bash
export PORT=8000
export APP_BASE_URL="http://127.0.0.1:8000"
export OPENAI_API_KEY="团队自己的 API Key"
export OPENAI_MODEL="gpt-4o-mini"
export OPENAI_API_URL="https://api.openai.com/v1/chat/completions"
export FEISHU_WEBHOOK_URL="团队自己的飞书机器人 Webhook"
export GOOGLE_CLIENT_ID="团队自己的 Google OAuth Client ID"
export GOOGLE_CLIENT_SECRET="团队自己的 Google OAuth Client Secret"
export GOOGLE_REDIRECT_URI="http://127.0.0.1:8000/auth/google/callback"
python3 app.py
```

不配置 `OPENAI_API_KEY` 时，系统会使用本地兜底分类器，仍可录入和测试基础流程。

## 3. 绑定 Gmail

第一版只创建 Gmail 草稿，不会自动发送邮件。

### Google Cloud 需要配置

1. 打开 Google Cloud Console。
2. 创建或选择一个项目。
3. 启用 Gmail API。
4. 进入 Google Auth Platform / OAuth 客户端。
5. 创建 OAuth 2.0 Client，类型选择 Web application。
6. Authorized redirect URI 填：

```text
http://127.0.0.1:8000/auth/google/callback
```

如果使用 8001 端口，则改成：

```text
http://127.0.0.1:8001/auth/google/callback
```

7. 把 Client ID 和 Client Secret 填入环境变量，或进入系统 `/settings` 保存。

### 系统内授权

1. 启动系统。
2. 打开 `设置`。
3. 点击 `连接 Gmail`。
4. 在 Google 授权页选择团队邮箱。
5. 授权成功后回到系统，设置页会显示 Gmail 已连接。

系统只申请：

```text
https://www.googleapis.com/auth/gmail.compose
```

也就是只创建草稿，不读取收件箱，不自动发送邮件。

## 4. 绑定飞书通知

1. 在飞书群添加自定义机器人。
2. 复制机器人 Webhook URL。
3. 设置环境变量：

```bash
export FEISHU_WEBHOOK_URL="飞书机器人 webhook"
```

触发通知的情况：

- AI 分类为询问报价/佣金
- AI 分类为要样品
- 需要人工处理
- AI 失败

飞书发送失败不会影响主流程，错误会写入错误日志。

## 5. 配置真实 AI

设置：

```bash
export OPENAI_API_KEY="团队自己的 API Key"
export OPENAI_MODEL="gpt-4o-mini"
export OPENAI_API_URL="https://api.openai.com/v1/chat/completions"
```

要求：

- AI 返回必须是严格 JSON。
- 报价、佣金、样品、物流、库存相关内容必须提示“需要人工确认”。
- AI 失败时原始回复不会丢失，会进入错误日志。

## 6. 日常使用流程

### A. 新建项目

1. 打开首页。
2. 点击 `新建项目`。
3. 填写产品名称、产品卖点、样品政策、佣金/报价规则、禁止承诺项、品牌语气。
4. 保存项目。

项目里的这些信息会作为生成首封邮件和回复草稿的上下文。

### B. 添加达人

1. 进入项目。
2. 在项目页面添加达人。
3. 建议填写：达人名称、邮箱、平台、主页链接。
4. 系统会在项目内显示每个达人的进度卡片。

### C. 生成首封邮件

1. 进入项目。
2. 对某个达人点击生成/重写首封邮件。
3. 打开达人详情检查邮件内容。
4. 确认无误后创建 Gmail 草稿。

### D. 创建 Gmail 草稿

前提：

- Gmail 已授权。
- 达人有邮箱。
- 达人已有首封邮件草稿或最终发送稿。

操作：

1. 在项目页或达人详情页点击 `Gmail 草稿`。
2. 系统会把邮件写入团队 Gmail 草稿箱。
3. 到 Gmail 草稿箱人工检查。
4. 人工确认后由运营人员在 Gmail 中发送。

系统不会自动发送邮件。

### E. 录入达人回复

1. 点击 `录入回复`。
2. 选择 Campaign。
3. 填写达人名称、平台、产品信息、达人原始回复。
4. 提交后系统会先保存原始回复，再做 AI 整理。
5. 进入详情页查看 AI 分类、中文摘要、下一步建议和回复草稿。

### F. 跟进进度

项目内达人阶段包括：

- 待首次触达
- 首封待确认
- 首次触达
- 已首次触达
- 首次回复
- 沟通环节
- 寄样品
- 拍摄环节
- 项目结束

运营人员可以在项目页使用 `前一步`、`下一步`、`完成` 更新阶段。

## 7. 错误日志怎么看

进入 `错误日志`：

- AI 错误：AI 接口失败或 JSON 解析失败。
- 飞书错误：Webhook 发送失败。
- Gmail 错误：OAuth、token 刷新、草稿创建失败。
- 系统错误：其他应用异常。

处理完可以标记为已解决。

## 8. 数据安全说明

`data/workspace.sqlite3` 是本地数据库，正式使用后会保存：

- Gmail access_token / refresh_token
- 达人邮箱
- 邮件草稿正文
- 达人回复
- AI 整理结果
- 错误日志

请不要把正式使用后的 `data/workspace.sqlite3` 上传到 GitHub 或公开网盘。

交付版当前数据库已脱敏，账号表、达人、邮件、回复和错误日志均为空。

## 9. 交付版当前状态

- 代码：已包含 AI、飞书、Gmail OAuth、Gmail 草稿、Campaign、达人进度、错误日志。
- 数据：保留 Campaign 示例配置；清空达人、回复、邮箱授权、草稿记录、错误日志。
- API：未配置。
- Gmail：未授权。
- 飞书：未配置。


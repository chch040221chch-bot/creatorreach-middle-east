# 中东达人定向邀约工作台

这是以 CreatorReach AI 为基础改造的本地应用。它用于：建立达人名单、保存公开资料与个性化切入点、生成阿语/英语邀约草稿、创建 Gmail 草稿、登记回复与推进合作阶段。

## 当前安全边界

- 默认只生成草稿；**不会自动发送邮件或社媒私信**。
- 创建 Gmail 草稿需要你在设置中完成 Google OAuth 授权；从 Gmail 发送仍由你手动确认。
- 产品是否可寄样、报价、佣金、广告授权、库存、配送、医疗器械注册和产品宣称，均需要人工确认。
- SQLite 数据库会存储达人商务联系方式、草稿与 Gmail 授权令牌；不要同步到公开网盘、GitHub 或聊天群。

## 启动

在此文件夹打开 PowerShell 后运行：

```powershell
python app.py
```

浏览器打开：

```text
http://127.0.0.1:8000
```

本应用没有第三方 Python 依赖；未配置 AI Key 时可用于建立项目、达人档案、编辑草稿和测试流程。配置 AI Key 后，系统可根据项目和达人资料生成首轮邀约、整理回复。

## 首次建一个中东项目

1. 打开“项目设置”。
2. 在“项目模板”选择 **沙特 / GCC 彩色隐形眼镜定向邀约**。
3. 填写真实品牌名称与已经确认的产品信息。不要把未确认的上市、寄样、价格或产品功效填为事实。
4. 保存项目后，进入项目页添加达人。
5. 每位达人至少填写：平台、主页链接、优先邀约语言、已核验的定制切入点。
6. 生成草稿后人工审核，再连接 Gmail 创建草稿。

## 达人筛选（新增）

打开 `/screening`。第一轮测试默认条件为全球（ALL）、100–1,000 粉、已核验、无已标记竞品冲突。
你可输入两位国家代码（例如 SA、US、TH）缩小范围，也可调整平台、粉丝区间、内容标签和核验状态，并在页面下方录入候选。清空粉丝数上下限可以测试其他规模。
如果要单独建立一轮测试项目，可在“项目设置”选择“全球美妆达人小规模测试（不发送）”模板。原沙特 / GCC 项目与数据不会被改动。
“已核验”要求填写具体国家代码、粉丝数、证据 URL 与观察日期；仍须人工确认来源是否真实且相关。使用全球筛选不等于所有候选均已核验。
录入后进入达人详情可修改这些字段。筛选只处理工作台已有记录，**不会从 TikTok 自动发现、抓取或群发邀约**。

## 最小达人建联流程

```text
待筛选 → 草稿待审核 → Gmail 草稿 → 人工发送 → 已回复 → 谈判
→ 确认合作 → 寄样/制作 → 内容审核 → 已发布 → 结算/复投
```

## Gmail 草稿配置

在 Google Cloud 中创建 Web 类型 OAuth 凭据，启用 Gmail API，并把以下地址设置为授权重定向 URI：

```text
http://127.0.0.1:8000/auth/google/callback
```

随后在应用的“设置”页填写 Client ID、Client Secret 和 Redirect URI，点击“连接 Gmail”。应用只请求创建草稿所需的 Gmail Compose 权限。

## 上线前检查

- 定制切入点是否来自达人的真实公开资料，而不是 AI 生成的假设；
- 阿语和英语版本的合作条件是否一致；
- 所有占位符、未确认价格与未确认寄样承诺是否已清除；
- 达人已拒绝或要求不再联系时，是否已停止后续跟进；
- 如果要寄样或付费，是否已将收件人信息和合同信息放在受控系统中。
